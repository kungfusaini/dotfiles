[opener]
zathura = [
    { run = 'zathura "$@"', desc = "Open with Zathura", for = "unix" }
]

[open]
prepend_rules = [
    { mime = "application/pdf", use = "zathura" },
    { name = "*.pdf", use = "zathura" }
]
